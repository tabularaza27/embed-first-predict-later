# Embed First, Predict Later

Slides for the PyData Amsterdam 2026 talk *Energy forecasting from weather embeddings*
by Kai Jeggle, Dexter Energy.

**Live deck:** https://tabularaza27.github.io/embed-first-predict-later/

Use the arrow keys to step through the slides. The deck is a single self-contained
`index.html` plus the media in `assets/`. This repository is published automatically
from the private source repository and contains only the rendered deck.
